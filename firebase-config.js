/**
 * 云端登录：把下方 apiKey 等改成 Firebase 控制台里的配置（可参考 firebase-config.example.js）。
 * 若 apiKey 留空，则仅用本机 localStorage，无需登录。
 */
window.firebaseConfig = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: "",
};
